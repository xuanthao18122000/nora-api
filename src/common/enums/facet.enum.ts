/**
 * Loại facet filter
 * 1: Single Select (Radio buttons)
 * 2: Multi Select (Checkboxes)
 * 3: Range (Slider)
 * 4: Boolean (Toggle)
 */
export enum FacetTypeEnum {
    SINGLE_SELECT = 1,
    MULTI_SELECT = 2,
    RANGE = 3,
    BOOLEAN = 4,
}
